name: Gradle Build
on:
  push:
jobs:
  Build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - uses: actions/setup-java-jdk@v4.7.1
        with:
            java-version: 21
      - name: Build
        run: ./gradlew check